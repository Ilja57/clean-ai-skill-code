---
name: out-of-scope
---

# Out of scope

A creature that fails the Animal domain gate — its description affirmatively contradicts the gate criterion (see `animal.md`), so it is, with certainty, outside our domain.

A determined result — "known to be outside", not "unknown" (that is Unresolved).
